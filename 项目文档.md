# 导航网站项目文档

## 1. 项目概述

### 1.1 项目简介
这是一个基于飞书多维表格的导航网站，支持PWA应用、多种皮肤主题、高性能缓存机制，以及一键部署到Vercel。

### 1.2 项目亮点
- **📱 PWA 应用**：支持离线访问和桌面安装，提供原生应用体验
- **🌙 多种皮肤主题**：自动跟随系统偏好的暗黑模式，支持手动切换，多个皮肤主题随意切换
- **⚡ 高性能**：智能缓存机制，快速响应，优秀的用户体验
- **🔧 易部署**：一键部署到Vercel，后续支持多种部署方式

### 1.3 应用场景
- 个人导航主页，方便快速访问常用网站
- 团队内部导航，统一管理团队常用工具和资源
- 学习资源导航，分类整理学习资料

## 2. 技术栈

### 2.1 后端技术
- **Node.js + Express**：服务器框架
- **axios**：HTTP客户端，用于调用飞书API
- **dotenv**：环境变量管理
- **lunar-javascript**：农历日期计算
- **moment**：日期时间处理

### 2.2 前端技术
- **原生JavaScript**：无框架依赖，轻量高效
- **CSS3**：现代CSS特性，支持CSS变量和动画
- **Bootstrap Icons**：图标库
- **响应式设计**：移动端优先的设计理念

### 2.3 API集成
- **飞书开放平台API**：多维表格数据获取
- **Google Favicon API**：自动获取网站图标

### 2.4 部署支持
- **Vercel**：已配置vercel.json，支持一键部署
- **PWA 支持**：满足 Progressive Web App 标准，支持离线访问和桌面安装

## 3. 项目结构

### 3.1 目录结构
```
├── public/                          # 静态资源目录
│   ├── css/
│   │   └── style.css               # 主样式文件
│   ├── js/
│   │   ├── app.js                  # 前端主入口
│   │   └── modules/                # 模块化代码
│   │       ├── core/               # 核心模块
│   │       │   ├── data-manager.js # 数据管理
│   │       │   ├── pwa-manager.js  # PWA管理
│   │       │   ├── theme-manager.js # 主题管理
│   │       │   └── ui-renderer.js  # UI渲染
│   │       ├── features/           # 功能模块
│   │       │   ├── interaction-manager.js # 交互管理
│   │       │   └── link-manager.js # 链接管理
│   │       └── utils/              # 工具模块
│   │           └── common-utils.js # 通用工具
│   ├── img/
│   │   ├── avatar.svg              # 用户头像图标
│   │   ├── favicon.ico             # 网站图标
│   │   └── icons/                  # PWA 图标资源目录
│   ├── manifest.json               # PWA Manifest 文件
│   ├── sw.js                       # Service Worker 脚本
│   └── index.html                  # 主页面
├── doc/                            # 文档目录
├── specs/                          # 规格说明
├── .env                            # 环境变量配置（不应提交到版本控制）
├── .env.example                    # 环境变量示例
├── package.json                    # 项目依赖配置
├── server.js                       # 服务器入口文件
└── vercel.json                     # Vercel部署配置
```

### 3.2 核心文件说明

| 文件路径 | 主要功能 |
|---------|---------|
| `server.js` | 后端服务器主入口，处理API请求和飞书集成 |
| `public/js/app.js` | 前端应用主入口，负责模块加载和初始化 |
| `public/js/modules/core/data-manager.js` | 数据管理，处理导航数据获取、缓存、默认数据等 |
| `public/js/modules/core/ui-renderer.js` | UI渲染，处理DOM元素渲染、工具项生成、分类菜单等 |
| `public/js/modules/core/theme-manager.js` | 主题管理，处理皮肤切换、暗黑模式等 |
| `public/js/modules/core/pwa-manager.js` | PWA管理，处理Service Worker注册、更新等 |
| `public/js/modules/features/link-manager.js` | 链接管理，处理添加、删除链接等功能 |

## 4. 核心功能

### 4.1 导航数据管理

#### 4.1.1 数据获取流程
1. 优先从LocalStorage读取缓存数据（缓存有效期1小时）
2. 如果缓存过期或不存在，调用飞书API获取最新数据
3. 如果API请求失败，使用默认数据作为备选

#### 4.1.2 数据结构
```javascript
{
  "success": true,
  "isMockData": false,
  "data": {
    "Code": [
      {
        "id": "record_id",
        "name": "GitHub",
        "url": "https://github.com",
        "category": "Code",
        "sort": 1,
        "icon": "bi-github"
      }
    ]
  },
  "categories": ["Code", "设计", "产品", "其它"],
  "timestamp": "2023-12-25T08:00:00.000Z",
  "dateInfo": {
    "time": "16:30",
    "date": "12月25日",
    "weekday": "星期一",
    "lunarDate": "腊月初五"
  }
}
```

### 4.2 主题管理

#### 4.2.1 主题切换机制
1. 自动检测系统主题偏好
2. 支持手动切换暗黑/亮色模式
3. 支持多种皮肤主题切换
4. 主题设置持久化到LocalStorage

#### 4.2.2 主题类型
- 亮色模式
- 暗黑模式
- 霓虹主题
- 更多主题变体

### 4.3 PWA功能

#### 4.3.1 PWA特性
- 离线访问支持
- 桌面安装支持
- 智能缓存策略
- 自动更新机制
- 跨平台支持

#### 4.3.2 Service Worker 功能
- 资源缓存
- 离线访问
- 后台同步
- 推送通知（预留）

### 4.4 链接管理

#### 4.4.1 添加链接
1. 填写网站名称、网址和分类
2. 自动获取网站图标
3. 保存到飞书多维表格

#### 4.4.2 删除链接
1. 点击工具项上的删除按钮
2. 确认删除操作
3. 从飞书多维表格中删除记录

### 4.5 搜索功能
- 支持按关键词搜索工具
- 支持按分类筛选工具
- 移动端适配的搜索体验

## 5. 代码结构与逻辑

### 5.1 后端代码逻辑

#### 5.1.1 服务器启动流程
1. 加载环境变量
2. 初始化Express应用
3. 设置静态文件服务
4. 定义API路由
5. 启动服务器监听

#### 5.1.2 API路由
| 路由 | 方法 | 功能 |
|------|------|------|
| `/api/navigation` | GET | 获取导航数据 |
| `/api/favicon` | GET | 获取网站图标 |
| `/api/links` | POST | 添加新的网站链接 |
| `/api/links/:id` | DELETE | 删除网站链接 |

#### 5.1.3 飞书API集成
1. 获取tenant_access_token（带缓存机制）
2. 调用飞书多维表格API获取数据
3. 调用飞书多维表格API创建/删除记录

### 5.2 前端代码逻辑

#### 5.2.1 应用初始化流程
1. 加载前端模块
2. 初始化核心模块（PWA、主题、数据、UI）
3. 初始化功能模块（链接管理、交互管理）
4. 获取并显示数据
5. 绑定事件监听器

#### 5.2.2 数据管理流程
1. `DataManager` 类负责数据的获取、缓存和管理
2. 优先从LocalStorage读取缓存数据
3. 如果缓存过期，调用API获取最新数据
4. 如果API请求失败，使用默认数据
5. 数据变更时触发`dataChanged`事件

#### 5.2.3 UI渲染流程
1. `UIRenderer` 类负责DOM元素的渲染
2. 生成分类菜单
3. 渲染工具项
4. 处理加载动画和错误信息
5. 更新时间和日期信息

#### 5.2.4 工具项渲染逻辑
1. 解析工具数据
2. 获取或生成工具图标
   - 优先使用数据中指定的图标
   - 其次尝试获取网站favicon
   - 最后生成文字图标作为备用
3. 创建工具项DOM元素
4. 添加交互事件

## 6. 业务流程

### 6.1 用户访问流程
```
用户访问网站 → 加载前端资源 → 初始化应用 → 显示加载动画 → 获取导航数据 → 渲染UI → 隐藏加载动画 → 响应用户交互
```

### 6.2 数据更新流程
```
用户添加/删除链接 → 调用API更新飞书多维表格 → 清除本地缓存 → 触发数据变更事件 → 重新获取数据 → 重新渲染UI
```

### 6.3 主题切换流程
```
用户切换主题 → 更新DOM属性 → 保存主题设置到LocalStorage → 重新渲染UI → 更新工具图标样式
```

## 7. 开发指南

### 7.1 环境配置

#### 7.1.1 前置条件
- Node.js 14.0+
- 飞书账号

#### 7.1.2 本地开发
1. 克隆项目代码
2. 安装依赖：`npm install`
3. 配置环境变量：复制`.env.example`为`.env`，并填写飞书应用信息
4. 启动开发服务器：`npm run dev`
5. 访问 `http://localhost:3000`

#### 7.1.3 环境变量说明
| 环境变量 | 说明 |
|---------|------|
| `APP_ID` | 飞书应用ID |
| `APP_SECRET` | 飞书应用密钥 |
| `APP_TOKEN` | 飞书多维表格Token |
| `TABLE_ID` | 飞书多维表格ID |
| `PORT` | 服务器端口，默认3000 |

### 7.2 代码规范
- 使用2空格缩进
- 遵循现有的代码风格
- 添加必要的注释
- 确保移动端兼容性
- 遵循安全最佳实践

### 7.3 开发流程
1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 8. 部署指南

### 8.1 Vercel部署（推荐）
1. 将代码推送到GitHub
2. 访问Vercel官网，使用GitHub账号登录
3. 点击"New Project"，选择要部署的仓库
4. 在"Environment Variables"中配置环境变量
5. 点击"Deploy"开始部署
6. 部署完成后，访问分配的域名

### 8.2 其他部署方式
- **Netlify**：配置类似Vercel，支持一键部署
- **传统服务器**：使用PM2等进程管理工具部署
- **Docker**：支持Docker容器化部署（预留）

## 9. 维护与故障排查

### 9.1 常见问题

#### 9.1.1 数据获取失败
- **症状**：页面显示“获取数据失败”或空白
- **解决方案**：
  1. 检查控制台错误
  2. 验证环境变量配置
  3. 测试API连接：访问 `http://localhost:3000/api/navigation`
  4. 检查飞书应用权限配置

#### 9.1.2 权限配置问题
- **症状**：API返回权限不足错误
- **解决方案**：
  1. 重新检查飞书应用权限配置
  2. 确保应用已发布（状态为“已启用”）
  3. 等待权限生效（通常需要1-2分钟）

#### 9.1.3 主题切换不生效
- **解决方案**：
  ```javascript
  // 清除浏览器缓存和本地存储
  localStorage.clear();
  // 然后刷新页面
  ```

### 9.2 日志与监控
- 服务器日志：通过`console.log`输出到控制台
- 前端日志：通过浏览器开发者工具查看
- 性能监控：前端集成了性能监控功能

## 10. 未来规划

### 10.1 功能规划
- 多语言支持（中英文切换）
- 随机切换背景图片
- 拖拽排序功能
- 更多主题选项
- 数据导入/导出功能
- 用户自定义分类

### 10.2 技术规划
- 支持更多API集成（如Notion、GitHub等）
- 支持多种部署方式
- 优化性能和用户体验
- 增强PWA功能

## 11. 贡献指南

欢迎提交Issue和Pull Request来改进这个项目！

### 11.1 提交Issue
- 清晰描述问题或功能需求
- 提供必要的截图或错误信息
- 说明使用环境和复现步骤

### 11.2 提交Pull Request
- 遵循代码规范
- 提供清晰的提交信息
- 确保所有功能正常工作
- 测试移动端兼容性

## 12. 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 13. 联系方式

- 项目地址：https://github.com/wubh2012/navsite
- 问题反馈：https://github.com/wubh2012/navsite/issues
- 功能建议：通过Issues提交功能需求

---

**文档版本**：v1.0.0  
**更新日期**：2025-11-28  
**文档作者**：自动生成